import { BackButton } from "../components/BackButton";

export const NotFound = () => {
    return (
        <>
            <BackButton />
            NotFound
        </>
    );
}

export default NotFound;